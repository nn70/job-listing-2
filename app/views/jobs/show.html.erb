<div class="text-center">
  <%= link_to("提供照片", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration:underline;") %>
</div>

<h1> <%= @job.title %> </h1>
  <div class="description bg-color4">
    <div class="descriptionContent">
      <%= simple_format(@job.description) %>
    </div>
  </div>
